<div class="d-flex flex-row justify-content-center m-5"> 
    <h1>{{ 'Bienvenue !' }}</h1>
</div>

<div class="d-flex flex-row justify-content-center m-5"> 
    <h5>{{ 'Retrouver ici tous les produits proposés sur notre site !' }}</h5>
</div>

<div class="d-flex flex-row justify-content-center m-5"> 
       <a class="btn btn-warning mt-2" href="{{ path('liste') }}">Accueil</a>
</div> 

<div> 
       <a class="btn btn-warning mt-2" href="{{ path('register') }}">Créer un compte</a>
       <a class="btn btn-warning mt-2" href="{{ path('app_login') }}">Se connecter</a>
</div> 

{% include 'alert.html.twig' %} 