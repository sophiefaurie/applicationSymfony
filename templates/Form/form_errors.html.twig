{%- block form_errors -%} 
    {%- if errors|length > 0 -%} 
    <div style="width:800px"> 
        {%- for error in errors -%} 
            <div class="alert alert-danger">{{ error.message }}</div> 
        {%- endfor -%} 
    </div> 
    {%- endif -%} 
{%- endblock form_errors -%} 